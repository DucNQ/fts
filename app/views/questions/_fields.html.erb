<%= render 'shared/error_messages', object: f.object %>

Subject:
<%= f.collection_select :subject_id, Subject.all, :id, :name %>

Type:
<% question_types = { S: "Single Choice", M: "Multiple Choice", T: "Text", E: "Excercise"} %> 
<%= f.select :type, question_types.map{|type| [type[1], type[0]]}%>

Content:
<%= f.text_area :content, size: "25x8"%>

Answers:
<%= f.text_field :a %>
<%= f.text_field :b %>
<%= f.text_field :c %>
<%= f.text_field :d %>